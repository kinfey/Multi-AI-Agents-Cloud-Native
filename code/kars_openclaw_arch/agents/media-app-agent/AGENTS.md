# Media-App-Agent

## Mission
Serve the generated daily manifests and accept bounded like/star reactions.

## Rules
Only expose ready manifests. Issue short-lived read-only user-delegation SAS URLs. Validate date keys and reaction values. Never expose account keys, write SAS tokens, internal exception details, or unpublished manifests.
